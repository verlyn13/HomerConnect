<html>
  <head>
    <meta charset="utf-8">
    <title>RSVP Confirmation for {{event.title}}</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #4a56e2;
        color: white;
        padding: 20px;
        text-align: center;
      }
      .content {
        padding: 20px;
        background-color: #f9f9f9;
      }
      .event-details {
        background-color: white;
        border-left: 4px solid #4a56e2;
        padding: 15px;
        margin: 20px 0;
      }
      .button {
        display: inline-block;
        background-color: #4a56e2;
        color: white;
        text-decoration: none;
        padding: 10px 20px;
        border-radius: 4px;
        margin: 20px 0;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 12px;
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>You're Going!</h1>
      </div>
      <div class="content">
        <h2>Hello, {{name}}!</h2>
        <p>You have successfully RSVP'd to attend the following event:</p>

        <div class="event-details">
          <h3>{{event.title}}</h3>
          <p><strong>Date:</strong> {{date}}</p>
          <p><strong>Time:</strong> {{time}}</p>
          <p><strong>Location:</strong> {{event.location}}</p>
          {{#if event.description}}
            <p>{{event.description}}</p>
          {{/if}}
        </div>

        <p>We've added this event to your calendar. You can view all your upcoming events on your profile page.</p>

        <a href="https://homerconnect.com/events/{{event.id}}" class="button">View Event Details</a>

        <p>We look forward to seeing you there!</p>
      </div>
      <div class="footer">
        <p>© 2025 Homer Calendar. All rights reserved.</p>
        <p>If you did not RSVP to this event, please <a href="https://homerconnect.com/contact">contact us</a>.</p>
      </div>
    </div>
  </body>
</html>
